DungNT RPG - model workspace

Items can use `model: <id>` in items.yml.
The plugin applies the Paper item_model key `dungntrpg:<id>` when available.

Example:
  model: dragon_slayer

This folder is a workspace for future custom model/resource-pack assets.
